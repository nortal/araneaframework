package org.araneaframework.framework;


/**
 * This interface marks the topmost flow context that everything else depends
 * on. One can use it to find the topmost flow context form the
 * {@link org.araneaframework.Environment}.
 */
public interface RootFlowContext extends FlowContext {

}
